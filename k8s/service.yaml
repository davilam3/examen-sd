apiVersion: v1
kind: Service

metadata:
  name: sd-cicd-u5

spec:
  type: NodePort

# El error intencional está aquí
  selector:
    app: webapp

  ports:
    - port: 80
      targetPort: 3000
      
# apiVersion: v1
# kind: Service
# metadata:
#   name: sd-cicd-u5
# spec:
#   type: NodePort
#   selector:
#     app: sd-cicd-u5
#   ports:
#     - port: 80
#       targetPort: 3000
